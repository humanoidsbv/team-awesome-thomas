import * as Styled from "./TimeEntries.styled";
import { TimeEntry } from "../time-entry/";

export const TimeEntries = () => {
  return (
    <Styled.TimeEntries>
      <TimeEntry />
    </Styled.TimeEntries>
  );
};
